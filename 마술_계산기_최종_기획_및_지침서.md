# 🔮 미스틱 캘큘레이터 (Mystic Calculator) 최종 기획 및 작동 지침서

본 지침서는 프로 마술사용 프리미엄 멘탈리즘 웹앱인 **"미스틱 캘큘레이터(Mystic Calculator)"**의 핵심 기획 내용, 모든 트릭의 상세 동작 사양, 시스템 구조 및 파일 명세를 하나로 집대성한 최종 문서입니다.

---

## 1. 프로젝트 개요 (Overview)
*   **도구명**: 미스틱 캘큘레이터 (Mystic Calculator)
*   **목적**: 스마트폰의 기본 계산기 앱(iOS / Android)으로 완벽하게 위장한 상태에서, 관객의 입력을 은밀하게 훔쳐보거나(Peeking), 원하는 숫자로 결과를 강제하고(Forcing), 페이크 인스타그램 게시물이나 무선 영수증 프린터로 예언을 실시간 출력하는 최상급 멘탈리즘 연출 도구.
*   **핵심 철학**: 어설픈 모조품이 아닌 100% 네이티브 UI 모사, 관객의 의심을 원천 차단하는 사각 없는 보안, 직관적이고 오작동 없는 마술사 제스처 제어.

---

## 2. 주요 트릭 동작 사양 (Magic Features)

### ① 포스 모드 (Force Mode)
*   **활성화 제스처**: 계산기 키패드의 `C` (또는 `AC`) 버튼을 **3초간 길게 터치**.
*   **피드백**: 마술사만 인지할 수 있도록 디스플레이 화면이 0.1초 동안 미세하게 어두워졌다가 원래 밝기로 복구(Visual Dimming)되며 미세 진동이 발생합니다.
*   **비밀 표식 (Marker)**: 포스 모드가 활성화되면 키패드의 `.`(소수점) 버튼 기호가 일반 마침표 `.`에서 가운데로 살짝 올라온 점 `·`으로 미세하게 바뀝니다. 마술사만 인지하는 스텔스 표식으로, 일반 관객은 소수점과 구분하기 어렵습니다. (구버전 기획에서는 `%` 버튼에 점을 표시했으나, 오인식·노출 위험을 줄이기 위해 `.` 버튼 기호 전환 방식으로 업그레이드됨.)
*   **작동**: 활성 상태에서 `=` 버튼을 누르면 화면에 입력된 수식과 무관하게 사전에 설정해 둔 강제 결과값이 도출됩니다.
*   **포스 유형**:
    *   *Static Number*: 마술사가 미리 지정해 둔 고정값 (예: `7777`).
    *   *Time-based Force*: 현재 시간 정보에 기반한 동적 값 생성 (예: YYYYMMDDHHMM 형식으로 오프셋 분을 가감하여 타임 포싱 가능).
    *   *포스 모드 해제*: 다시 `C` 버튼을 3초간 누르거나, 1회성 포스(One-time) 설정 시 `=` 입력 즉시 자동 해제됩니다.

### ② 비밀 기록 훔쳐보기 (Spectator Peeking)
*   **활성화 제스처**: 키패드 `9` 버튼을 **1초간 길게 터치**.
*   **제어**: 1초 도달 시 디밍 피드백이 발생하며 Peeking 모드가 켜지고, 손가락이 닿아 있는 채로 곧바로 드래그(좌측 스와이프)로 이어집니다. 터치를 뗄 때 숫자 `9`가 디스플레이에 입력되지 않도록 안전하게 무시됩니다. (구버전 기획의 2초 → 빠른 연출을 위해 1초로 업그레이드됨.)
*   **훔쳐보기 모션**: 화면을 오른쪽에서 왼쪽으로 쓸어넘기면(Swipe Left), 상단의 계산기 화면 레이어가 마술사 손가락을 따라 물리적으로 밀려나며(Spring Motion) 뒤쪽에 가려져 있던 고대비 검은색 배경의 **숫자 입력 히스토리 로그(History Log)**가 노출됩니다.
*   **로그 필터**: 연산자나 결과값은 필터링되고, 관객이 입력한 순수 원본 숫자들만 누적 수집되어 깔끔하게 보입니다.
*   **자동 복원**: 손가락을 화면에서 떼면 0.1초 만에 전면 계산기 레이어가 부드럽게 원위치로 닫히며 감춰집니다.
*   **로그 청소**: 키패드 `=` 버튼을 **2초간 길게 터치**하면 누적 수집된 Peeking 로그가 완전히 지워집니다.

### ③ Impossible Erase (좌측부터 한 글자씩 삭제)
*   **활성화 제스처**: 키패드의 `+/-` 버튼을 터치하여 활성화합니다.
*   **작동**: 버튼명이 **`-/+`**로 치환되며 트릭이 켜집니다. 이 상태에서 계산기 결과 표시창을 **오른쪽에서 왼쪽으로 스와이프(좌측으로 쓸기)**하면, 일반 계산기와 달리 **가장 왼쪽(앞자리) 숫자부터 한 글자씩 자연스럽게 페이드아웃되며 삭제**됩니다. (스와이프 1회당 앞자리 1글자.)

### ④ 결과값 즉시 복원 (Result Recovery)
*   **작동**: 수식이 초기화된 상태(`0` 또는 `AC` 상태)에서 `=` 버튼을 짧게 터치하면, 직전에 연산 완료되었던 최종 결과값이 디스플레이에 마법처럼 다시 나타납니다.

---

## 3. 예언 연동 및 확장 기능 (Prophecy Integrations)

### ① 페이크 인스타그램 예언 모프 (Morph to Instagram)
*   **진입 트리거**: 계산기 화면에 **`0000 =`**을 차례로 입력합니다.
*   **스플래시 화면**: 진입 즉시 1.5초 동안 검은 바탕에 무지개 그라디언트 아이콘과 Meta 로고가 뜨는 **실제 인스타그램 모바일 앱 스플래시 화면**이 노출되어 진짜 앱이 켜지는 착각을 유도합니다.
*   **가짜 피드 구성**:
    *   마술사 인스타 계정(`mystic_lab_magic`)의 단독 포스트 화면이 다크모드로 표시됩니다.
    *   게시물 업로드 일자는 교묘하게 **"3주 전(3 weeks ago)"**으로 소급 표기되어 있습니다.
    *   **동적 캡션 변환**: 본문 내용 중 *"3주 전 꿈속에서 이상한 세 숫자를 보았다..."*라는 설명 사이에 관객이 방금 전에 계산기에 입력했던 숫자(`num1`, `num2`)와 최종 결과값(`result`)이 실시간 매핑되어 노출됩니다.
    *   **인터랙션 피드백**: 관객이 좋아요(하트) 아이콘을 누르면 하트가 빨갛게 차오르고 좋아요 숫자가 1 증가하며 미세 햅틱 진동이 발생하는 등 완전한 네이티브 인앱 인터랙션을 보여줍니다.
    *   **탈출**: 좌상단 뒤로가기 버튼(`<`)을 누르면 즉시 계산기 홈화면으로 회귀합니다.

### ② 감열식 블루투스 프린터 예언 출력
*   **작동**: 계산기 `=` 버튼을 **3초간 길게 터치** 시 연동된 블루투스 감열식 프린터로 영수증이 실시간 출력됩니다.
*   **필기체 그래픽 렌더링**: 프린터 자체 폰트 대신, 가상 HTML5 Canvas를 활용하여 Google Fonts의 영수증 전용 필기체/필기 폰트(Nanum Pen Script, Caveat 등)로 텍스트를 그린 뒤, 이를 흑백 모노크롬 비트맵 이미지 이진 데이터(`GS v 0` 커맨드)로 변환해 무선 송신합니다.
*   **iOS 가이드**: Web Bluetooth를 공식 지원하지 않는 아이폰 유저들을 위해, 전용 블루투스 브라우저 앱인 **'Bluefy'** 내에서 구동하는 매뉴얼을 설정창에 포함합니다.

---

## 4. 기기 전용 보안 인증 시스템 (Single Device Security)

*   **동작 개념**: 1개의 구매 코드당 동시에 딱 1개의 모바일 단말기만 작동 권한을 가집니다.
*   **메커니즘**:
    1.  인증 코드를 입력하면 고유한 암호화 무작위 토큰(SHA-256 해시)이 신규 생성되어 Supabase DB의 `product_unlock_codes.active_token_hash` 컬럼에 갱신됩니다.
    2.  해당 토큰은 로그인한 스마트폰의 브라우저 쿠키와 `localStorage`에 암호화 보관됩니다.
    3.  마술 도중 계산기에 접속하거나 연산할 때마다 실시간 비교하여, **새로운 스마트폰에서 동일 코드로 등록을 진행하는 순간 기존 스마트폰의 활성 세션 토큰은 무효화** 처리됩니다.
    4.  권한이 만료된 기기는 즉시 차단 팝업이 노출되며 마술 도구 화면에서 강제 퇴출됩니다.

---

## 5. 최신화된 파일 폴더 구조 명세 (v16)

기존 웹페이지 구성 파일들과 격리하여 깔끔하게 독립 관리될 수 있도록 루트의 `magic/` 폴더 아래로 모든 소스코드를 집약 배치하였습니다.

```
mystic-lab/
├── magic/                         <-- [마술 코어 폴더] 모든 소스코드 응축
│   ├── components/                <-- 마술 기능용 프론트엔드 UI 컴포넌트
│   │   ├── MagicCalculator.tsx    (사칙연산, 포스 마커, 블루투스 프린팅 모듈)
│   │   ├── FakeInstagram.tsx      (페이크 인스타 피드 구현 및 햅틱 연동)
│   │   └── ClientPwaWrapper.tsx   (PWA 브라우저 감지 및 설치 튜토리얼 팝업)
│   ├── pages/                     <-- 실시간 기기 가드 로직 내장 페이지 컴포넌트
│   │   ├── CalculatorPage.tsx     (계산기 메인 페이지 논리)
│   │   └── InstagramPage.tsx      (페이크 인스타 상세 페이지 논리)
│   └── api/                       <-- 백엔드 라이선스 처리 로직
│       └── verify-license.ts      (SHA-256 토큰 생성 및 단말기 갱신 갱신)
│
├── app/                           <-- [연결 브릿지] 라우팅용 엔트리 (가벼운 export 코드)
│   ├── [locale]/
│   │   └── calc/
│   │       ├── page.tsx           ➔ magic/pages/CalculatorPage 호출
│   │       └── instagram/
│   │           └── page.tsx       ➔ magic/pages/InstagramPage 호출
│   └── api/
│       └── magic/
│           └── verify-license/
│               └── route.ts       ➔ magic/api/verify-license 호출
│
└── public/
    ├── manifest.json              * PWA 매니페스트 (아이콘 경로 격리 완료)
    └── images/
        └── magic/                 * 마술 에셋 전용 독립 폴더
            ├── calc-icon-192.png  (위장 계산기 PWA 실행 아이콘 192px)
            ├── calc-icon-512.png  (위장 계산기 PWA 실행 아이콘 512px)
            ├── instagram-icon.png (가짜 인스타 실행 아이콘)
            └── instagram-post.png (페이크 포스트용 고화질 타로카드 이미지)
```
